import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test placeholder', (WidgetTester tester) async {
    // Full integration tests require a running Isar instance.
    // Run the app with `flutter run` for manual testing.
    expect(true, isTrue);
  });
}
